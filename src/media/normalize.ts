/**
 * Image normalization, re-exported at the path this app imports it from —
 * the implementation is the SDK's, shared with every other transport.
 */
export { VISION_MAX_DIMENSION, normalizeImageForChat } from "@assistant-hub-swarm/transport-sdk";
